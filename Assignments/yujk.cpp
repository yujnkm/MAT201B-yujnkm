#include "al/core.hpp"
#include "al/util/al_Image.hpp"
using namespace al;

struct MyApp : App
                   Mesh

                   nav()
                       .pullBack(7);

Image::RGBAPix auto &vertexList = mesh.vertices();
for (color c : colorList)
    position[1].push_back(Vector)
        position
        Vec3f v;
v.x = ;
v /